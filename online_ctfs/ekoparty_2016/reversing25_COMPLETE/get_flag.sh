#!/bin/bash

# take the original .class file and decode with ShowMyCode!
# read the source to determine that the flag is really
# EKO{} with the sum(range(1337)) on the inside.
# you can modify the Java code to print out the string s
# if you want (change all `string` types to `String`)

java EKO
